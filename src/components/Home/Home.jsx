import React from 'react';

import { Hero, FeaturedProducts, OurProducts } from '../../components';

const Home = () => {
    return (
        <div>
            <Hero />
            <FeaturedProducts />
            <OurProducts />
        </div>
    );
};

export default Home;
